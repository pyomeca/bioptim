# create a dev conda environment with: `make create_env` or `conda env create -f environment.yml`
name: bioptim
channels:
- conda-forge
dependencies:
- biorbd >=1.11.1
- numpy >=2.4,<3
- matplotlib
- pyqt
- pyqtgraph
- python-graphviz
