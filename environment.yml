# create a dev conda environment with: `make create_env` or `conda env create -f environment.yml`
name: bioptim
channels:
- conda-forge
- default
dependencies:
- python >=3.9
- biorbd >=1.8.8
- bioviz
- scipy
- numpy
- packaging
- python-graphviz
- pyqtgraph
- pygmo

