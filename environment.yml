# create a dev conda environment with: `make create_env` or `conda env create -f environment.yml`
name: bioptim
channels:
- conda-forge
- default
dependencies:
- python
- numpy
- matplotlib
- casadi
- rbdl =*=*casadi*
- biorbd =*=*casadi*
- bioviz =*=*casadi*
- scipy
- packaging
