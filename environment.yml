# create a dev conda environment with: `make create_env` or `conda env create -f environment.yml`
name: bioptim
channels:
- conda-forge
- default
dependencies:
- biorbd >=1.7.2
- bioviz
- scipy
- numpy
- packaging
- python-graphviz
